import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev overlay sits on top of full-bleed type and skews specimen captures.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
